//! RGBA
#[derive(Debug)]
/// Defines colors with an alpha channel.
pub struct RGBA {
    /// The red value.
    pub r: u8,
    /// The green value.
    pub g: u8,
    /// The blue value.
    pub b: u8,
    /// The alpha value.
    pub a: u8,
}
